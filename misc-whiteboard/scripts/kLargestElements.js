const Heap = require('../classes/BinaryMinHeap');
const heapSort = require('./heapSort');

const kLargestElements = inputArray => {
  // Your code here

  return [];
};

module.exports = kLargestElements;
